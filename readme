Querido Professor,

Tentamos deixar o migrate do laravel automático com o arquivo entrypoint, porém não consegui.

É necessário rodar:

docker exec -it laravel-app php artisan migrate

assim irá subir as tabelas do banco.
